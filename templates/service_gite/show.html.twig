{% extends 'base.html.twig' %}

{% block title %}ServiceGite{% endblock %}

{% block body %}
    <h1>ServiceGite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_gite.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ service_gite.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_gite_index') }}">back to list</a>

    <a href="{{ path('app_service_gite_edit', {'id': service_gite.id}) }}">edit</a>

    {{ include('service_gite/_delete_form.html.twig') }}
{% endblock %}
